import type { IArtifact } from '@/models/interfaces/IArtifact';
import type { IItem } from '@/models/interfaces/IItem';

export const processArtifactCreation = (item: IItem): IArtifact[] => {
  const artifacts: IArtifact[] = [];

  // TODO: Implement artifact creation processing

  return artifacts;
};
